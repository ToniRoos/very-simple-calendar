import { DateRange, NumberRange } from "../types";
export declare const isDateInRange: (range: DateRange, date: Date) => boolean;
export declare const isInRange: (range: NumberRange, number: number) => boolean;
